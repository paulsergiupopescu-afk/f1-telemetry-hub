# Maintenance scripts

`import_brendon_setups.py` regenerates the normalized runtime JSON from the
original setup package. It is a maintenance tool, not part of normal startup.

Run it from the repository root. Review the resulting JSON and execute the setup
tests before building the application.
